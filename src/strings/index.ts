import { sv } from './sv';

export type { Strings } from './sv';

/**
 * Aktivt språk. Byt här — eller läs av användarens språk — när engelska
 * läggs till. Komponenterna importerar bara `strings`.
 */
export const strings = sv;
